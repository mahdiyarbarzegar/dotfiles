# ============================================================
# PATH
# ============================================================

path=(
    "$HOME/.local/bin"
    "/opt/nvim"
    "$HOME/bin"
    $path
)

export PATH
